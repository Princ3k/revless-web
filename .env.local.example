# Copy to .env.local for local development
# On Vercel, set these in Project Settings → Environment Variables

# URL of the Revless FastAPI backend (no trailing slash)
NEXT_PUBLIC_API_URL=https://revless-api.onrender.com/api/v1
